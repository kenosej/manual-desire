using System;
using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEditor;
using UnityEngine;

namespace Map.OpponentAI
{
    public class Waypoints : MonoBehaviour
    {
        //private List<Vector3> _gizmos = new List<Vector3>();
        //private void OnDrawGizmos()
        //{
        //    for (int i = 0; i < this.transform.childCount; i++)
        //    {
        //        var obj = this.transform.GetChild(i);
        //        for (int j = 0; j < obj.transform.childCount; j++)
        //        {
        //            if (obj.transform.GetChild(j).CompareTag("Waypoint"))
        //            {
        //                Gizmos.color = Color.blue;
        //                Gizmos.DrawSphere(obj.transform.GetChild(j).transform.position,1f);
        //                _gizmos.Add(obj.transform.GetChild(j).transform.position);
        //            }
        //        }
        //    }
//
        //    Gizmos.color = Color.red;
        //    for (int i = 0; i < _gizmos.Count-1; i++)
        //    {
        //        Gizmos.DrawLine(_gizmos[i], _gizmos[i+1]);
        //    }
        //}
    }
}

